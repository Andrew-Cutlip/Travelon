import React from 'react'

function feature(props) {
    return(
        <>
            <button onClick={props.onClick}><a href={"/Join now"}>Join Now</a></button>
        </>
    )
}

export default feature